[nosetests]
logging-format = %(pathname)s:%(lineno)d: %(levelname)s %(message)s

verbosity = 1

no-byte-compile = 1

with-coverage = 1
cover-package = crumbs
cover-erase = 1

with-doctest = 1
doctest-tests = 1
